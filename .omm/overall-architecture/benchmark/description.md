benchmark/는 프로파일, 러너, 지표 계산, 유효성, schema 3 저장, baseline·비교 로직을 포함합니다. BenchmarkActivity와 ThermalTracker는 Android 의존성이 있고 순수 계산 로직과 분리됩니다. 실행 완료는 RunAssembler와 BenchmarkReport까지 연결되며, ResultPresenter 비교 UI는 Activity에서 아직 호출되지 않습니다.
