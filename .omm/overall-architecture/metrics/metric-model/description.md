Doctor 시대의 모델에서 M3 이후에 남은 세 가지 선언을 담고 있습니다. `UnknownReason` 은 측정 불가, 실행 안됨, 표본 부족, 기준 없음, 지원 안됨,Cadence 변경, 조건 불일치 등의 이유를, `MetricSample` 은 비교 전의 단일 측정값 (value, p95, n, hardFailure, unknownReason, expectedMs, fixedCadence, timeout) 을 나타냅니다.