title: HALCamera 개발자 가이드
description: Camera HAL 개발자를 위한 HALCamera 앱 가이드
lang: ko

# 밑줄로 시작하는 파일과 폴더는 Jekyll 이 기본으로 제외합니다.
# _inputs 는 설계 결정과 기기 검증 기록을 사람이 읽을 수 있도록 포함합니다.
include:
  - _inputs
exclude:
  - _bindings.yaml
  - _content
